# gpg

> *GNU Privacy Guard for R*

Bindings to GPG for creating and verifying OpenGPG (RFC4880) signatures.
This is not a standalone library; GPG needs to be installed on the
system. On Windows you need GPG4Win or similar, on other systems use the
GPGME library.

## Documentation

About the R package:

- Vignette: [Using GPG in
  R](https://cran.r-project.org/web/packages/gpg/vignettes/intro.html)

Other resources:

- [The GNU Privacy Handbook](https://www.gnupg.org/gph/en/manual.html)

## Hello World

Let’s verify a Debian file. The [Debian page on
CRAN](https://cran.r-project.org/bin/linux/debian/) says the following:

*Since 16th of November 2021, the buster40 and bullseye40 repositories
are signed with a new key with the key ID 0xB8F25A8A73EACF41,
fingerprint 95C0FAF38DB3CCAD0C080A7BDC78B2DDEABC47B7 and user ID
Johannes Ranke <johannes.ranke@jrwb.de>.*

We import this key so that we can verify the
[Release](https://cran.r-project.org/bin/linux/debian/bullseye-cran40/Release)
file, which contains checksums for all files in the repository:

``` r

# take out the spaces
johannes <- "0xB8F25A8A73EACF41"
gpg_recv(johannes)
```

If you don’t trust the CRAN homepage, you could check who has signed
this key. You’d need to import the corresponding peer keys for more
information.

``` r

gpg_list_signatures(johannes)
```

Now lets verify the release files:

``` r

# Verify the file
library(curl)
curl_download('https://cran.r-project.org/bin/linux/debian/bullseye-cran40/Release', 'Release')
curl_download('https://cran.r-project.org/bin/linux/debian/bullseye-cran40/Release.gpg','Release.gpg')
gpg_verify('Release.gpg', 'Release')
```

                    id           timestamp           name                  email success
    1 DC78B2DDEABC47B7 2021-11-16 11:17:18 Johannes Ranke johannes.ranke@jrwb.de    TRUE

Looking good! We can trust the checksums in the `Release` file to be
legitimate.

## Installation

Binary packages for **OS-X** or **Windows** can be installed directly
from CRAN:

``` r

install.packages("gpg")
```

Installation from source on Linux or OSX requires
[`GPGME`](https://www.gnupg.org/(es)/related_software/gpgme/index.html).
On **Debian** or **Ubuntu** install
[libgpgme-dev](https://packages.debian.org/testing/libgpgme-dev)
directly from Universe:

    sudo apt-get install -y libgpgme-dev

On **Fedora** and **CentOS** we need
[gpgme-devel](https://src.fedoraproject.org/rpms/gpgme):

    sudo yum install gpgme-devel

On **OS-X** use
[gpgme](https://github.com/Homebrew/homebrew-core/blob/master/Formula/gpgme.rb)
from Homebrew:

    brew install gpgme

# Package index

## All functions

- [`gpg_encrypt()`](https://docs.ropensci.org/gpg/reference/gpg_encrypt.md)
  [`gpg_decrypt()`](https://docs.ropensci.org/gpg/reference/gpg_encrypt.md)
  : Encryption
- [`gpg_restart()`](https://docs.ropensci.org/gpg/reference/gpg_info.md)
  [`gpg_version()`](https://docs.ropensci.org/gpg/reference/gpg_info.md)
  [`gpg_info()`](https://docs.ropensci.org/gpg/reference/gpg_info.md)
  [`gpg_options()`](https://docs.ropensci.org/gpg/reference/gpg_info.md)
  : Manage the GPG engine
- [`gpg_keygen()`](https://docs.ropensci.org/gpg/reference/gpg_keygen.md)
  : GPG key generation
- [`gpg_import()`](https://docs.ropensci.org/gpg/reference/gpg_keys.md)
  [`gpg_recv()`](https://docs.ropensci.org/gpg/reference/gpg_keys.md)
  [`gpg_send()`](https://docs.ropensci.org/gpg/reference/gpg_keys.md)
  [`gpg_delete()`](https://docs.ropensci.org/gpg/reference/gpg_keys.md)
  [`gpg_export()`](https://docs.ropensci.org/gpg/reference/gpg_keys.md)
  [`gpg_list_keys()`](https://docs.ropensci.org/gpg/reference/gpg_keys.md)
  [`gpg_list_signatures()`](https://docs.ropensci.org/gpg/reference/gpg_keys.md)
  : GPG keyring management
- [`gpg_verify()`](https://docs.ropensci.org/gpg/reference/gpg_sign.md)
  [`gpg_sign()`](https://docs.ropensci.org/gpg/reference/gpg_sign.md) :
  PGP Signatures
- [`pinentry()`](https://docs.ropensci.org/gpg/reference/pinentry.md) :
  Password Entry

# Articles

### All vignettes

- [Encryption and Digital Signatures using
  GPG](https://docs.ropensci.org/gpg/articles/intro.md):
